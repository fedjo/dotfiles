# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
	name = yiorgos marinellis
	email = yiorgos@deevio.ai
	signingkey = F5C3D86FE8A982CB
[alias]
	lg = log --color --graph --all --pretty=format:'%C(yellow)%h%Creset -%C(auto)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset %G?' --abbrev-commit
    lbD = fetch -p && git branch -vv | awk '/: gone]/{print $1}' | xargs git branch -d
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
